/*
 * Public, non-secret configuration for the landing-page handoff.
 * Set this only after Zenzo Cloud has been published to a stable HTTPS URL.
 * Never put provider keys, desktop installation tokens, or bot secrets here.
 */
window.ZENZO_CLOUD_URL = '';
window.ZENZO_OX_ALPHA_DAILY_ALLOWANCE = 1_000_000;
